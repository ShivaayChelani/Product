module.exports = {
  project: {
    ios: {},
    android: {},
  },
  // Custom fonts (Playfair / Inter). Folder may be empty until TTFs are added.
  // Vector icon fonts are shipped by RNVectorIcons.podspec + Android fonts.gradle.
  assets: ['./assets/fonts/'],
  dependencies: {},
};
